[
  {
    "id": "ts_read_file",
    "tags": ["typescript", "fs"],
    "prompt": "Read the file `src/config.json` and tell me what port it configures.",
    "expect": { "name": "read_file" },
    "require_args": ["path"],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "read_file",
          "description": "Read a file from disk and return its contents as text.",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string", "description": "Path to the file, relative to the project root." }
            },
            "required": ["path"]
          }
        }
      }
    ]
  },
  {
    "id": "ts_list_dir",
    "tags": ["typescript", "fs"],
    "prompt": "What files are in the `src/components` directory?",
    "expect": { "name": "list_directory" },
    "require_args": ["path"],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "list_directory",
          "description": "List the entries of a directory.",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string", "description": "Directory path relative to the project root." }
            },
            "required": ["path"]
          }
        }
      }
    ]
  },
  {
    "id": "ts_grep",
    "tags": ["typescript", "search"],
    "prompt": "Find every place in the repo where `createServer` is called.",
    "expect": { "name": "grep_search" },
    "require_args": ["pattern"],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "grep_search",
          "description": "Search file contents with a regular expression and return matching locations.",
          "parameters": {
            "type": "object",
            "properties": {
              "pattern": { "type": "string", "description": "Regular expression to search for." },
              "path": { "type": "string", "description": "Optional subdirectory to restrict the search." },
              "glob": { "type": "string", "description": "Optional file glob filter." }
            },
            "required": ["pattern"]
          }
        }
      }
    ]
  },
  {
    "id": "ts_edit_file",
    "tags": ["typescript", "edit"],
    "prompt": "Change the hardcoded timeout of 3000ms in `src/api/client.ts` to 10000ms.",
    "expect": { "name": "edit_file" },
    "require_args": ["path"],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "edit_file",
          "description": "Apply a text replacement to a file.",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string" },
              "old_string": { "type": "string" },
              "new_string": { "type": "string" }
            },
            "required": ["path", "old_string", "new_string"]
          }
        }
      }
    ]
  },
  {
    "id": "ts_run_tests",
    "tags": ["typescript", "shell"],
    "prompt": "Run the test suite and tell me if it passes.",
    "expect": { "name": "run_command" },
    "require_args": ["command"],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "run_command",
          "description": "Execute a shell command in the project root and return stdout, stderr and the exit code.",
          "parameters": {
            "type": "object",
            "properties": {
              "command": { "type": "string", "description": "The shell command to run." },
              "timeout_ms": { "type": "integer" }
            },
            "required": ["command"]
          }
        }
      }
    ]
  },
  {
    "id": "ts_multi_step",
    "tags": ["typescript", "agentic", "planning"],
    "prompt": "The build is failing after the last commit. Investigate and fix it.",
    "expect": { "name": "read_file" },
    "require_args": ["path"],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "read_file",
          "description": "Read a file from disk.",
          "parameters": {
            "type": "object",
            "properties": { "path": { "type": "string" } },
            "required": ["path"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "run_command",
          "description": "Run a shell command.",
          "parameters": {
            "type": "object",
            "properties": { "command": { "type": "string" } },
            "required": ["command"]
          }
        }
      },
      {
        "type": "function",
        "function": {
          "name": "grep_search",
          "description": "Search file contents.",
          "parameters": {
            "type": "object",
            "properties": { "pattern": { "type": "string" } },
            "required": ["pattern"]
          }
        }
      }
    ]
  },
  {
    "id": "ts_types",
    "tags": ["typescript", "types"],
    "prompt": "Add a `createdAt: string` field to the `User` interface in `src/types/user.ts`.",
    "expect": { "name": "edit_file" },
    "require_args": ["path"],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "edit_file",
          "description": "Apply a text replacement to a file.",
          "parameters": {
            "type": "object",
            "properties": {
              "path": { "type": "string" },
              "old_string": { "type": "string" },
              "new_string": { "type": "string" }
            },
            "required": ["path"]
          }
        }
      }
    ]
  },
  {
    "id": "ts_webfetch",
    "tags": ["typescript", "web"],
    "prompt": "Fetch the contents of https://example.com/docs/api and summarise the endpoints.",
    "expect": { "name": "web_fetch" },
    "require_args": ["url"],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "web_fetch",
          "description": "Fetch a URL and return the response body as text.",
          "parameters": {
            "type": "object",
            "properties": { "url": { "type": "string" } },
            "required": ["url"]
          }
        }
      }
    ]
  }
]
